<!DOCTYPE html>
<html>
    <head>
        <title>Date</title>
    </head>
    <body>
        <% today = DateTime.new %>
        <h1>Today</h1>
        <p>The date is <%= today.strftime('%A, %d %b, %Y') %></p>
    </body>
</html>